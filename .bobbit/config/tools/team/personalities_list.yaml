name: personalities_list
description: "List all available personalities"
summary: "List all available personalities that can be applied to spawned
  agents. Each personality modifies how the agent approaches its work."
provider:
  type: bobbit-extension
  extension: extension.ts
group: Team
renderer: src/ui/tools/renderers/PersonalityToolRenderers.ts
docs: |-
  No parameters. Returns all available personality definitions.
detail_docs: |
  List all available personalities that can be applied to spawned agents.
  Each personality modifies the agent's behavior via system prompt additions.

  No parameters required. Returns an array of personality objects with
  name, description, and prompt fields.
